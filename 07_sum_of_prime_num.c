#include<stdio.h> //working not complet
int main()
{
    int num,a=0,isprime;
        printf("Enter the limit of number : ");
        scanf("%d",&num);
        for(int i=0;i<=num;i++)
        {
            isprime=1;
            if(i%a==0)
            {

            }
        }
    return 0;
}